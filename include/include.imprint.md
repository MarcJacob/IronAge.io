# INCLUDE FOLDER

Main include folder useable across the project. This is how the various subsystems talk to eachother and common symbols are shared widely.

The */core/* folder contains includes that should be included pretty much everywhere in the codebase. *core.h* is a shorthand include for doing exactly that.

*/game_server/* contains game-server-specific includes.